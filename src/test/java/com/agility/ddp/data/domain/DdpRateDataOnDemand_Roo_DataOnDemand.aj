// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpRate;
import com.agility.ddp.data.domain.DdpRateDataOnDemand;
import com.agility.ddp.data.domain.DdpRateRepository;
import com.agility.ddp.data.domain.DdpRateService;
import com.agility.ddp.data.domain.DdpRuleDetail;
import com.agility.ddp.data.domain.DdpRuleDetailDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect DdpRateDataOnDemand_Roo_DataOnDemand {
    
    declare @type: DdpRateDataOnDemand: @Component;
    
    private Random DdpRateDataOnDemand.rnd = new SecureRandom();
    
    private List<DdpRate> DdpRateDataOnDemand.data;
    
    @Autowired
    DdpRuleDetailDataOnDemand DdpRateDataOnDemand.ddpRuleDetailDataOnDemand;
    
    @Autowired
    DdpRateService DdpRateDataOnDemand.ddpRateService;
    
    @Autowired
    DdpRateRepository DdpRateDataOnDemand.ddpRateRepository;
    
    public DdpRate DdpRateDataOnDemand.getNewTransientDdpRate(int index) {
        DdpRate obj = new DdpRate();
        setRtsCreatedBy(obj, index);
        setRtsCreatedDate(obj, index);
        setRtsModifiedBy(obj, index);
        setRtsModifiedDate(obj, index);
        setRtsOption(obj, index);
        setRtsRdtId(obj, index);
        return obj;
    }
    
    public void DdpRateDataOnDemand.setRtsCreatedBy(DdpRate obj, int index) {
        String rtsCreatedBy = "rtsCreatedBy_" + index;
        if (rtsCreatedBy.length() > 120) {
            rtsCreatedBy = rtsCreatedBy.substring(0, 120);
        }
        obj.setRtsCreatedBy(rtsCreatedBy);
    }
    
    public void DdpRateDataOnDemand.setRtsCreatedDate(DdpRate obj, int index) {
        String rtsCreatedDate = "rtsCreatedDate_" + index;
        if (rtsCreatedDate.length() > 120) {
            rtsCreatedDate = rtsCreatedDate.substring(0, 120);
        }
        obj.setRtsCreatedDate(rtsCreatedDate);
    }
    
    public void DdpRateDataOnDemand.setRtsModifiedBy(DdpRate obj, int index) {
        String rtsModifiedBy = "rtsModifiedBy_" + index;
        if (rtsModifiedBy.length() > 120) {
            rtsModifiedBy = rtsModifiedBy.substring(0, 120);
        }
        obj.setRtsModifiedBy(rtsModifiedBy);
    }
    
    public void DdpRateDataOnDemand.setRtsModifiedDate(DdpRate obj, int index) {
        String rtsModifiedDate = "rtsModifiedDate_" + index;
        if (rtsModifiedDate.length() > 120) {
            rtsModifiedDate = rtsModifiedDate.substring(0, 120);
        }
        obj.setRtsModifiedDate(rtsModifiedDate);
    }
    
    public void DdpRateDataOnDemand.setRtsOption(DdpRate obj, int index) {
        String rtsOption = "rtsOption_" + index;
        if (rtsOption.length() > 16) {
            rtsOption = rtsOption.substring(0, 16);
        }
        obj.setRtsOption(rtsOption);
    }
    
    public void DdpRateDataOnDemand.setRtsRdtId(DdpRate obj, int index) {
        DdpRuleDetail rtsRdtId = ddpRuleDetailDataOnDemand.getRandomDdpRuleDetail();
        obj.setRtsRdtId(rtsRdtId);
    }
    
    public DdpRate DdpRateDataOnDemand.getSpecificDdpRate(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        DdpRate obj = data.get(index);
        Integer id = obj.getRtsId();
        return ddpRateService.findDdpRate(id);
    }
    
    public DdpRate DdpRateDataOnDemand.getRandomDdpRate() {
        init();
        DdpRate obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getRtsId();
        return ddpRateService.findDdpRate(id);
    }
    
    public boolean DdpRateDataOnDemand.modifyDdpRate(DdpRate obj) {
        return false;
    }
    
    public void DdpRateDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ddpRateService.findDdpRateEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'DdpRate' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<DdpRate>();
        for (int i = 0; i < 10; i++) {
            DdpRate obj = getNewTransientDdpRate(i);
            try {
                ddpRateService.saveDdpRate(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            ddpRateRepository.flush();
            data.add(obj);
        }
    }
    
}
