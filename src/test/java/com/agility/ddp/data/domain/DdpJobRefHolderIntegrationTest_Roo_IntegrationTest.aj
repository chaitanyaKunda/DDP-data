// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpJobRefHolderDataOnDemand;
import com.agility.ddp.data.domain.DdpJobRefHolderIntegrationTest;
import com.agility.ddp.data.domain.DdpJobRefHolderPK;
import com.agility.ddp.data.domain.DdpJobRefHolderRepository;
import com.agility.ddp.data.domain.DdpJobRefHolderService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpJobRefHolderIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpJobRefHolderIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpJobRefHolderIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpJobRefHolderIntegrationTest: @Transactional;
    
    @Autowired
    DdpJobRefHolderDataOnDemand DdpJobRefHolderIntegrationTest.dod;
    
    @Autowired
    DdpJobRefHolderService DdpJobRefHolderIntegrationTest.ddpJobRefHolderService;
    
    @Autowired
    DdpJobRefHolderRepository DdpJobRefHolderIntegrationTest.ddpJobRefHolderRepository;
    
    @Test
    public void DdpJobRefHolderIntegrationTest.testCountAllDdpJobRefHolders() {
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to initialize correctly", dod.getRandomDdpJobRefHolder());
        long count = ddpJobRefHolderService.countAllDdpJobRefHolders();
        Assert.assertTrue("Counter for 'DdpJobRefHolder' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpJobRefHolderIntegrationTest.testFindDdpJobRefHolder() {
        DdpJobRefHolder obj = dod.getRandomDdpJobRefHolder();
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to initialize correctly", obj);
        DdpJobRefHolderPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to provide an identifier", id);
        obj = ddpJobRefHolderService.findDdpJobRefHolder(id);
        Assert.assertNotNull("Find method for 'DdpJobRefHolder' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpJobRefHolder' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DdpJobRefHolderIntegrationTest.testFindAllDdpJobRefHolders() {
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to initialize correctly", dod.getRandomDdpJobRefHolder());
        long count = ddpJobRefHolderService.countAllDdpJobRefHolders();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpJobRefHolder', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpJobRefHolder> result = ddpJobRefHolderService.findAllDdpJobRefHolders();
        Assert.assertNotNull("Find all method for 'DdpJobRefHolder' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpJobRefHolder' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpJobRefHolderIntegrationTest.testFindDdpJobRefHolderEntries() {
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to initialize correctly", dod.getRandomDdpJobRefHolder());
        long count = ddpJobRefHolderService.countAllDdpJobRefHolders();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpJobRefHolder> result = ddpJobRefHolderService.findDdpJobRefHolderEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpJobRefHolder' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpJobRefHolder' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpJobRefHolderIntegrationTest.testSaveDdpJobRefHolder() {
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to initialize correctly", dod.getRandomDdpJobRefHolder());
        DdpJobRefHolder obj = dod.getNewTransientDdpJobRefHolder(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to provide a new transient entity", obj);
        try {
            ddpJobRefHolderService.saveDdpJobRefHolder(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpJobRefHolderRepository.flush();
        Assert.assertNotNull("Expected 'DdpJobRefHolder' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DdpJobRefHolderIntegrationTest.testDeleteDdpJobRefHolder() {
        DdpJobRefHolder obj = dod.getRandomDdpJobRefHolder();
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to initialize correctly", obj);
        DdpJobRefHolderPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DdpJobRefHolder' failed to provide an identifier", id);
        obj = ddpJobRefHolderService.findDdpJobRefHolder(id);
        ddpJobRefHolderService.deleteDdpJobRefHolder(obj);
        ddpJobRefHolderRepository.flush();
        Assert.assertNull("Failed to remove 'DdpJobRefHolder' with identifier '" + id + "'", ddpJobRefHolderService.findDdpJobRefHolder(id));
    }
    
}
