// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpExportMissingDocsDataOnDemand;
import com.agility.ddp.data.domain.DdpExportMissingDocsIntegrationTest;
import com.agility.ddp.data.domain.DdpExportMissingDocsRepository;
import com.agility.ddp.data.domain.DdpExportMissingDocsService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpExportMissingDocsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpExportMissingDocsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpExportMissingDocsIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpExportMissingDocsIntegrationTest: @Transactional;
    
    @Autowired
    DdpExportMissingDocsDataOnDemand DdpExportMissingDocsIntegrationTest.dod;
    
    @Autowired
    DdpExportMissingDocsService DdpExportMissingDocsIntegrationTest.ddpExportMissingDocsService;
    
    @Autowired
    DdpExportMissingDocsRepository DdpExportMissingDocsIntegrationTest.ddpExportMissingDocsRepository;
    
    @Test
    public void DdpExportMissingDocsIntegrationTest.testCountAllDdpExportMissingDocses() {
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to initialize correctly", dod.getRandomDdpExportMissingDocs());
        long count = ddpExportMissingDocsService.countAllDdpExportMissingDocses();
        Assert.assertTrue("Counter for 'DdpExportMissingDocs' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpExportMissingDocsIntegrationTest.testFindDdpExportMissingDocs() {
        DdpExportMissingDocs obj = dod.getRandomDdpExportMissingDocs();
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to initialize correctly", obj);
        Integer id = obj.getMisId();
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to provide an identifier", id);
        obj = ddpExportMissingDocsService.findDdpExportMissingDocs(id);
        Assert.assertNotNull("Find method for 'DdpExportMissingDocs' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpExportMissingDocs' returned the incorrect identifier", id, obj.getMisId());
    }
    
    @Test
    public void DdpExportMissingDocsIntegrationTest.testFindAllDdpExportMissingDocses() {
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to initialize correctly", dod.getRandomDdpExportMissingDocs());
        long count = ddpExportMissingDocsService.countAllDdpExportMissingDocses();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpExportMissingDocs', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpExportMissingDocs> result = ddpExportMissingDocsService.findAllDdpExportMissingDocses();
        Assert.assertNotNull("Find all method for 'DdpExportMissingDocs' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpExportMissingDocs' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpExportMissingDocsIntegrationTest.testFindDdpExportMissingDocsEntries() {
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to initialize correctly", dod.getRandomDdpExportMissingDocs());
        long count = ddpExportMissingDocsService.countAllDdpExportMissingDocses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpExportMissingDocs> result = ddpExportMissingDocsService.findDdpExportMissingDocsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpExportMissingDocs' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpExportMissingDocs' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpExportMissingDocsIntegrationTest.testSaveDdpExportMissingDocs() {
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to initialize correctly", dod.getRandomDdpExportMissingDocs());
        DdpExportMissingDocs obj = dod.getNewTransientDdpExportMissingDocs(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpExportMissingDocs' identifier to be null", obj.getMisId());
        try {
            ddpExportMissingDocsService.saveDdpExportMissingDocs(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpExportMissingDocsRepository.flush();
        Assert.assertNotNull("Expected 'DdpExportMissingDocs' identifier to no longer be null", obj.getMisId());
    }
    
    @Test
    public void DdpExportMissingDocsIntegrationTest.testDeleteDdpExportMissingDocs() {
        DdpExportMissingDocs obj = dod.getRandomDdpExportMissingDocs();
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to initialize correctly", obj);
        Integer id = obj.getMisId();
        Assert.assertNotNull("Data on demand for 'DdpExportMissingDocs' failed to provide an identifier", id);
        obj = ddpExportMissingDocsService.findDdpExportMissingDocs(id);
        ddpExportMissingDocsService.deleteDdpExportMissingDocs(obj);
        ddpExportMissingDocsRepository.flush();
        Assert.assertNull("Failed to remove 'DdpExportMissingDocs' with identifier '" + id + "'", ddpExportMissingDocsService.findDdpExportMissingDocs(id));
    }
    
}
