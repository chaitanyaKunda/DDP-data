// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpRateLkpDataOnDemand;
import com.agility.ddp.data.domain.DdpRateLkpIntegrationTest;
import com.agility.ddp.data.domain.DdpRateLkpPK;
import com.agility.ddp.data.domain.DdpRateLkpRepository;
import com.agility.ddp.data.domain.DdpRateLkpService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpRateLkpIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpRateLkpIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpRateLkpIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpRateLkpIntegrationTest: @Transactional;
    
    @Autowired
    DdpRateLkpDataOnDemand DdpRateLkpIntegrationTest.dod;
    
    @Autowired
    DdpRateLkpService DdpRateLkpIntegrationTest.ddpRateLkpService;
    
    @Autowired
    DdpRateLkpRepository DdpRateLkpIntegrationTest.ddpRateLkpRepository;
    
    @Test
    public void DdpRateLkpIntegrationTest.testCountAllDdpRateLkps() {
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to initialize correctly", dod.getRandomDdpRateLkp());
        long count = ddpRateLkpService.countAllDdpRateLkps();
        Assert.assertTrue("Counter for 'DdpRateLkp' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpRateLkpIntegrationTest.testFindDdpRateLkp() {
        DdpRateLkp obj = dod.getRandomDdpRateLkp();
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to initialize correctly", obj);
        DdpRateLkpPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to provide an identifier", id);
        obj = ddpRateLkpService.findDdpRateLkp(id);
        Assert.assertNotNull("Find method for 'DdpRateLkp' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpRateLkp' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DdpRateLkpIntegrationTest.testFindAllDdpRateLkps() {
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to initialize correctly", dod.getRandomDdpRateLkp());
        long count = ddpRateLkpService.countAllDdpRateLkps();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpRateLkp', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpRateLkp> result = ddpRateLkpService.findAllDdpRateLkps();
        Assert.assertNotNull("Find all method for 'DdpRateLkp' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpRateLkp' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpRateLkpIntegrationTest.testFindDdpRateLkpEntries() {
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to initialize correctly", dod.getRandomDdpRateLkp());
        long count = ddpRateLkpService.countAllDdpRateLkps();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpRateLkp> result = ddpRateLkpService.findDdpRateLkpEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpRateLkp' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpRateLkp' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpRateLkpIntegrationTest.testSaveDdpRateLkp() {
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to initialize correctly", dod.getRandomDdpRateLkp());
        DdpRateLkp obj = dod.getNewTransientDdpRateLkp(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to provide a new transient entity", obj);
        try {
            ddpRateLkpService.saveDdpRateLkp(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpRateLkpRepository.flush();
        Assert.assertNotNull("Expected 'DdpRateLkp' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DdpRateLkpIntegrationTest.testDeleteDdpRateLkp() {
        DdpRateLkp obj = dod.getRandomDdpRateLkp();
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to initialize correctly", obj);
        DdpRateLkpPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DdpRateLkp' failed to provide an identifier", id);
        obj = ddpRateLkpService.findDdpRateLkp(id);
        ddpRateLkpService.deleteDdpRateLkp(obj);
        ddpRateLkpRepository.flush();
        Assert.assertNull("Failed to remove 'DdpRateLkp' with identifier '" + id + "'", ddpRateLkpService.findDdpRateLkp(id));
    }
    
}
