// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpRulePmSvcDataOnDemand;
import com.agility.ddp.data.domain.DdpRulePmSvcIntegrationTest;
import com.agility.ddp.data.domain.DdpRulePmSvcRepository;
import com.agility.ddp.data.domain.DdpRulePmSvcService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpRulePmSvcIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpRulePmSvcIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpRulePmSvcIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpRulePmSvcIntegrationTest: @Transactional;
    
    @Autowired
    DdpRulePmSvcDataOnDemand DdpRulePmSvcIntegrationTest.dod;
    
    @Autowired
    DdpRulePmSvcService DdpRulePmSvcIntegrationTest.ddpRulePmSvcService;
    
    @Autowired
    DdpRulePmSvcRepository DdpRulePmSvcIntegrationTest.ddpRulePmSvcRepository;
    
    @Test
    public void DdpRulePmSvcIntegrationTest.testCountAllDdpRulePmSvcs() {
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to initialize correctly", dod.getRandomDdpRulePmSvc());
        long count = ddpRulePmSvcService.countAllDdpRulePmSvcs();
        Assert.assertTrue("Counter for 'DdpRulePmSvc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpRulePmSvcIntegrationTest.testFindDdpRulePmSvc() {
        DdpRulePmSvc obj = dod.getRandomDdpRulePmSvc();
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to initialize correctly", obj);
        Integer id = obj.getRpsId();
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to provide an identifier", id);
        obj = ddpRulePmSvcService.findDdpRulePmSvc(id);
        Assert.assertNotNull("Find method for 'DdpRulePmSvc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpRulePmSvc' returned the incorrect identifier", id, obj.getRpsId());
    }
    
    @Test
    public void DdpRulePmSvcIntegrationTest.testFindAllDdpRulePmSvcs() {
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to initialize correctly", dod.getRandomDdpRulePmSvc());
        long count = ddpRulePmSvcService.countAllDdpRulePmSvcs();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpRulePmSvc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpRulePmSvc> result = ddpRulePmSvcService.findAllDdpRulePmSvcs();
        Assert.assertNotNull("Find all method for 'DdpRulePmSvc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpRulePmSvc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpRulePmSvcIntegrationTest.testFindDdpRulePmSvcEntries() {
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to initialize correctly", dod.getRandomDdpRulePmSvc());
        long count = ddpRulePmSvcService.countAllDdpRulePmSvcs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpRulePmSvc> result = ddpRulePmSvcService.findDdpRulePmSvcEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpRulePmSvc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpRulePmSvc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpRulePmSvcIntegrationTest.testSaveDdpRulePmSvc() {
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to initialize correctly", dod.getRandomDdpRulePmSvc());
        DdpRulePmSvc obj = dod.getNewTransientDdpRulePmSvc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpRulePmSvc' identifier to be null", obj.getRpsId());
        try {
            ddpRulePmSvcService.saveDdpRulePmSvc(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpRulePmSvcRepository.flush();
        Assert.assertNotNull("Expected 'DdpRulePmSvc' identifier to no longer be null", obj.getRpsId());
    }
    
    @Test
    public void DdpRulePmSvcIntegrationTest.testDeleteDdpRulePmSvc() {
        DdpRulePmSvc obj = dod.getRandomDdpRulePmSvc();
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to initialize correctly", obj);
        Integer id = obj.getRpsId();
        Assert.assertNotNull("Data on demand for 'DdpRulePmSvc' failed to provide an identifier", id);
        obj = ddpRulePmSvcService.findDdpRulePmSvc(id);
        ddpRulePmSvcService.deleteDdpRulePmSvc(obj);
        ddpRulePmSvcRepository.flush();
        Assert.assertNull("Failed to remove 'DdpRulePmSvc' with identifier '" + id + "'", ddpRulePmSvcService.findDdpRulePmSvc(id));
    }
    
}
