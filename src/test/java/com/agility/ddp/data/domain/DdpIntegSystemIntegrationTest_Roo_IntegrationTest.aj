// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpIntegSystemDataOnDemand;
import com.agility.ddp.data.domain.DdpIntegSystemIntegrationTest;
import com.agility.ddp.data.domain.DdpIntegSystemRepository;
import com.agility.ddp.data.domain.DdpIntegSystemService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpIntegSystemIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpIntegSystemIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpIntegSystemIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpIntegSystemIntegrationTest: @Transactional;
    
    @Autowired
    DdpIntegSystemDataOnDemand DdpIntegSystemIntegrationTest.dod;
    
    @Autowired
    DdpIntegSystemService DdpIntegSystemIntegrationTest.ddpIntegSystemService;
    
    @Autowired
    DdpIntegSystemRepository DdpIntegSystemIntegrationTest.ddpIntegSystemRepository;
    
    @Test
    public void DdpIntegSystemIntegrationTest.testCountAllDdpIntegSystems() {
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to initialize correctly", dod.getRandomDdpIntegSystem());
        long count = ddpIntegSystemService.countAllDdpIntegSystems();
        Assert.assertTrue("Counter for 'DdpIntegSystem' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpIntegSystemIntegrationTest.testFindDdpIntegSystem() {
        DdpIntegSystem obj = dod.getRandomDdpIntegSystem();
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to initialize correctly", obj);
        String id = obj.getInsApplicationCode();
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to provide an identifier", id);
        obj = ddpIntegSystemService.findDdpIntegSystem(id);
        Assert.assertNotNull("Find method for 'DdpIntegSystem' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpIntegSystem' returned the incorrect identifier", id, obj.getInsApplicationCode());
    }
    
    @Test
    public void DdpIntegSystemIntegrationTest.testFindAllDdpIntegSystems() {
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to initialize correctly", dod.getRandomDdpIntegSystem());
        long count = ddpIntegSystemService.countAllDdpIntegSystems();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpIntegSystem', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpIntegSystem> result = ddpIntegSystemService.findAllDdpIntegSystems();
        Assert.assertNotNull("Find all method for 'DdpIntegSystem' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpIntegSystem' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpIntegSystemIntegrationTest.testFindDdpIntegSystemEntries() {
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to initialize correctly", dod.getRandomDdpIntegSystem());
        long count = ddpIntegSystemService.countAllDdpIntegSystems();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpIntegSystem> result = ddpIntegSystemService.findDdpIntegSystemEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpIntegSystem' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpIntegSystem' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpIntegSystemIntegrationTest.testSaveDdpIntegSystem() {
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to initialize correctly", dod.getRandomDdpIntegSystem());
        DdpIntegSystem obj = dod.getNewTransientDdpIntegSystem(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpIntegSystem' identifier to be null", obj.getInsApplicationCode());
        try {
            ddpIntegSystemService.saveDdpIntegSystem(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpIntegSystemRepository.flush();
        Assert.assertNotNull("Expected 'DdpIntegSystem' identifier to no longer be null", obj.getInsApplicationCode());
    }
    
    @Test
    public void DdpIntegSystemIntegrationTest.testDeleteDdpIntegSystem() {
        DdpIntegSystem obj = dod.getRandomDdpIntegSystem();
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to initialize correctly", obj);
        String id = obj.getInsApplicationCode();
        Assert.assertNotNull("Data on demand for 'DdpIntegSystem' failed to provide an identifier", id);
        obj = ddpIntegSystemService.findDdpIntegSystem(id);
        ddpIntegSystemService.deleteDdpIntegSystem(obj);
        ddpIntegSystemRepository.flush();
        Assert.assertNull("Failed to remove 'DdpIntegSystem' with identifier '" + id + "'", ddpIntegSystemService.findDdpIntegSystem(id));
    }
    
}
