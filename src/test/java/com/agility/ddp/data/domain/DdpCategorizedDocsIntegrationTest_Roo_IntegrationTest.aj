// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpCategorizedDocsDataOnDemand;
import com.agility.ddp.data.domain.DdpCategorizedDocsIntegrationTest;
import com.agility.ddp.data.domain.DdpCategorizedDocsRepository;
import com.agility.ddp.data.domain.DdpCategorizedDocsService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpCategorizedDocsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpCategorizedDocsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpCategorizedDocsIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpCategorizedDocsIntegrationTest: @Transactional;
    
    @Autowired
    DdpCategorizedDocsDataOnDemand DdpCategorizedDocsIntegrationTest.dod;
    
    @Autowired
    DdpCategorizedDocsService DdpCategorizedDocsIntegrationTest.ddpCategorizedDocsService;
    
    @Autowired
    DdpCategorizedDocsRepository DdpCategorizedDocsIntegrationTest.ddpCategorizedDocsRepository;
    
    @Test
    public void DdpCategorizedDocsIntegrationTest.testCountAllDdpCategorizedDocses() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to initialize correctly", dod.getRandomDdpCategorizedDocs());
        long count = ddpCategorizedDocsService.countAllDdpCategorizedDocses();
        Assert.assertTrue("Counter for 'DdpCategorizedDocs' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpCategorizedDocsIntegrationTest.testFindDdpCategorizedDocs() {
        DdpCategorizedDocs obj = dod.getRandomDdpCategorizedDocs();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to initialize correctly", obj);
        Integer id = obj.getCatId();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to provide an identifier", id);
        obj = ddpCategorizedDocsService.findDdpCategorizedDocs(id);
        Assert.assertNotNull("Find method for 'DdpCategorizedDocs' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpCategorizedDocs' returned the incorrect identifier", id, obj.getCatId());
    }
    
    @Test
    public void DdpCategorizedDocsIntegrationTest.testFindAllDdpCategorizedDocses() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to initialize correctly", dod.getRandomDdpCategorizedDocs());
        long count = ddpCategorizedDocsService.countAllDdpCategorizedDocses();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpCategorizedDocs', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpCategorizedDocs> result = ddpCategorizedDocsService.findAllDdpCategorizedDocses();
        Assert.assertNotNull("Find all method for 'DdpCategorizedDocs' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpCategorizedDocs' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpCategorizedDocsIntegrationTest.testFindDdpCategorizedDocsEntries() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to initialize correctly", dod.getRandomDdpCategorizedDocs());
        long count = ddpCategorizedDocsService.countAllDdpCategorizedDocses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpCategorizedDocs> result = ddpCategorizedDocsService.findDdpCategorizedDocsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpCategorizedDocs' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpCategorizedDocs' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpCategorizedDocsIntegrationTest.testSaveDdpCategorizedDocs() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to initialize correctly", dod.getRandomDdpCategorizedDocs());
        DdpCategorizedDocs obj = dod.getNewTransientDdpCategorizedDocs(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpCategorizedDocs' identifier to be null", obj.getCatId());
        try {
            ddpCategorizedDocsService.saveDdpCategorizedDocs(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpCategorizedDocsRepository.flush();
        Assert.assertNotNull("Expected 'DdpCategorizedDocs' identifier to no longer be null", obj.getCatId());
    }
    
    @Test
    public void DdpCategorizedDocsIntegrationTest.testDeleteDdpCategorizedDocs() {
        DdpCategorizedDocs obj = dod.getRandomDdpCategorizedDocs();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to initialize correctly", obj);
        Integer id = obj.getCatId();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDocs' failed to provide an identifier", id);
        obj = ddpCategorizedDocsService.findDdpCategorizedDocs(id);
        ddpCategorizedDocsService.deleteDdpCategorizedDocs(obj);
        ddpCategorizedDocsRepository.flush();
        Assert.assertNull("Failed to remove 'DdpCategorizedDocs' with identifier '" + id + "'", ddpCategorizedDocsService.findDdpCategorizedDocs(id));
    }
    
}
