// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpAuditTxnDataOnDemand;
import com.agility.ddp.data.domain.DdpAuditTxnIntegrationTest;
import com.agility.ddp.data.domain.DdpAuditTxnRepository;
import com.agility.ddp.data.domain.DdpAuditTxnService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpAuditTxnIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpAuditTxnIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpAuditTxnIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpAuditTxnIntegrationTest: @Transactional;
    
    @Autowired
    DdpAuditTxnDataOnDemand DdpAuditTxnIntegrationTest.dod;
    
    @Autowired
    DdpAuditTxnService DdpAuditTxnIntegrationTest.ddpAuditTxnService;
    
    @Autowired
    DdpAuditTxnRepository DdpAuditTxnIntegrationTest.ddpAuditTxnRepository;
    
    @Test
    public void DdpAuditTxnIntegrationTest.testCountAllDdpAuditTxns() {
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to initialize correctly", dod.getRandomDdpAuditTxn());
        long count = ddpAuditTxnService.countAllDdpAuditTxns();
        Assert.assertTrue("Counter for 'DdpAuditTxn' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpAuditTxnIntegrationTest.testFindDdpAuditTxn() {
        DdpAuditTxn obj = dod.getRandomDdpAuditTxn();
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to initialize correctly", obj);
        Integer id = obj.getAtxId();
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to provide an identifier", id);
        obj = ddpAuditTxnService.findDdpAuditTxn(id);
        Assert.assertNotNull("Find method for 'DdpAuditTxn' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpAuditTxn' returned the incorrect identifier", id, obj.getAtxId());
    }
    
    @Test
    public void DdpAuditTxnIntegrationTest.testFindAllDdpAuditTxns() {
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to initialize correctly", dod.getRandomDdpAuditTxn());
        long count = ddpAuditTxnService.countAllDdpAuditTxns();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpAuditTxn', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpAuditTxn> result = ddpAuditTxnService.findAllDdpAuditTxns();
        Assert.assertNotNull("Find all method for 'DdpAuditTxn' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpAuditTxn' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpAuditTxnIntegrationTest.testFindDdpAuditTxnEntries() {
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to initialize correctly", dod.getRandomDdpAuditTxn());
        long count = ddpAuditTxnService.countAllDdpAuditTxns();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpAuditTxn> result = ddpAuditTxnService.findDdpAuditTxnEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpAuditTxn' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpAuditTxn' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpAuditTxnIntegrationTest.testSaveDdpAuditTxn() {
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to initialize correctly", dod.getRandomDdpAuditTxn());
        DdpAuditTxn obj = dod.getNewTransientDdpAuditTxn(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpAuditTxn' identifier to be null", obj.getAtxId());
        try {
            ddpAuditTxnService.saveDdpAuditTxn(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpAuditTxnRepository.flush();
        Assert.assertNotNull("Expected 'DdpAuditTxn' identifier to no longer be null", obj.getAtxId());
    }
    
    @Test
    public void DdpAuditTxnIntegrationTest.testDeleteDdpAuditTxn() {
        DdpAuditTxn obj = dod.getRandomDdpAuditTxn();
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to initialize correctly", obj);
        Integer id = obj.getAtxId();
        Assert.assertNotNull("Data on demand for 'DdpAuditTxn' failed to provide an identifier", id);
        obj = ddpAuditTxnService.findDdpAuditTxn(id);
        ddpAuditTxnService.deleteDdpAuditTxn(obj);
        ddpAuditTxnRepository.flush();
        Assert.assertNull("Failed to remove 'DdpAuditTxn' with identifier '" + id + "'", ddpAuditTxnService.findDdpAuditTxn(id));
    }
    
}
