// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpGroupDataOnDemand;
import com.agility.ddp.data.domain.DdpGroupIntegrationTest;
import com.agility.ddp.data.domain.DdpGroupRepository;
import com.agility.ddp.data.domain.DdpGroupService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpGroupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpGroupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpGroupIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpGroupIntegrationTest: @Transactional;
    
    @Autowired
    DdpGroupDataOnDemand DdpGroupIntegrationTest.dod;
    
    @Autowired
    DdpGroupService DdpGroupIntegrationTest.ddpGroupService;
    
    @Autowired
    DdpGroupRepository DdpGroupIntegrationTest.ddpGroupRepository;
    
    @Test
    public void DdpGroupIntegrationTest.testCountAllDdpGroups() {
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to initialize correctly", dod.getRandomDdpGroup());
        long count = ddpGroupService.countAllDdpGroups();
        Assert.assertTrue("Counter for 'DdpGroup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpGroupIntegrationTest.testFindDdpGroup() {
        DdpGroup obj = dod.getRandomDdpGroup();
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to initialize correctly", obj);
        Integer id = obj.getGrpId();
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to provide an identifier", id);
        obj = ddpGroupService.findDdpGroup(id);
        Assert.assertNotNull("Find method for 'DdpGroup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpGroup' returned the incorrect identifier", id, obj.getGrpId());
    }
    
    @Test
    public void DdpGroupIntegrationTest.testFindAllDdpGroups() {
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to initialize correctly", dod.getRandomDdpGroup());
        long count = ddpGroupService.countAllDdpGroups();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpGroup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpGroup> result = ddpGroupService.findAllDdpGroups();
        Assert.assertNotNull("Find all method for 'DdpGroup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpGroup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpGroupIntegrationTest.testFindDdpGroupEntries() {
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to initialize correctly", dod.getRandomDdpGroup());
        long count = ddpGroupService.countAllDdpGroups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpGroup> result = ddpGroupService.findDdpGroupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpGroup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpGroup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpGroupIntegrationTest.testSaveDdpGroup() {
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to initialize correctly", dod.getRandomDdpGroup());
        DdpGroup obj = dod.getNewTransientDdpGroup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpGroup' identifier to be null", obj.getGrpId());
        try {
            ddpGroupService.saveDdpGroup(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpGroupRepository.flush();
        Assert.assertNotNull("Expected 'DdpGroup' identifier to no longer be null", obj.getGrpId());
    }
    
    @Test
    public void DdpGroupIntegrationTest.testDeleteDdpGroup() {
        DdpGroup obj = dod.getRandomDdpGroup();
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to initialize correctly", obj);
        Integer id = obj.getGrpId();
        Assert.assertNotNull("Data on demand for 'DdpGroup' failed to provide an identifier", id);
        obj = ddpGroupService.findDdpGroup(id);
        ddpGroupService.deleteDdpGroup(obj);
        ddpGroupRepository.flush();
        Assert.assertNull("Failed to remove 'DdpGroup' with identifier '" + id + "'", ddpGroupService.findDdpGroup(id));
    }
    
}
