// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpRoleDataOnDemand;
import com.agility.ddp.data.domain.DdpRoleIntegrationTest;
import com.agility.ddp.data.domain.DdpRoleRepository;
import com.agility.ddp.data.domain.DdpRoleService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpRoleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpRoleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpRoleIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpRoleIntegrationTest: @Transactional;
    
    @Autowired
    DdpRoleDataOnDemand DdpRoleIntegrationTest.dod;
    
    @Autowired
    DdpRoleService DdpRoleIntegrationTest.ddpRoleService;
    
    @Autowired
    DdpRoleRepository DdpRoleIntegrationTest.ddpRoleRepository;
    
    @Test
    public void DdpRoleIntegrationTest.testCountAllDdpRoles() {
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to initialize correctly", dod.getRandomDdpRole());
        long count = ddpRoleService.countAllDdpRoles();
        Assert.assertTrue("Counter for 'DdpRole' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpRoleIntegrationTest.testFindDdpRole() {
        DdpRole obj = dod.getRandomDdpRole();
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to initialize correctly", obj);
        Integer id = obj.getRolId();
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to provide an identifier", id);
        obj = ddpRoleService.findDdpRole(id);
        Assert.assertNotNull("Find method for 'DdpRole' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpRole' returned the incorrect identifier", id, obj.getRolId());
    }
    
    @Test
    public void DdpRoleIntegrationTest.testFindAllDdpRoles() {
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to initialize correctly", dod.getRandomDdpRole());
        long count = ddpRoleService.countAllDdpRoles();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpRole', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpRole> result = ddpRoleService.findAllDdpRoles();
        Assert.assertNotNull("Find all method for 'DdpRole' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpRole' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpRoleIntegrationTest.testFindDdpRoleEntries() {
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to initialize correctly", dod.getRandomDdpRole());
        long count = ddpRoleService.countAllDdpRoles();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpRole> result = ddpRoleService.findDdpRoleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpRole' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpRole' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpRoleIntegrationTest.testSaveDdpRole() {
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to initialize correctly", dod.getRandomDdpRole());
        DdpRole obj = dod.getNewTransientDdpRole(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpRole' identifier to be null", obj.getRolId());
        try {
            ddpRoleService.saveDdpRole(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpRoleRepository.flush();
        Assert.assertNotNull("Expected 'DdpRole' identifier to no longer be null", obj.getRolId());
    }
    
    @Test
    public void DdpRoleIntegrationTest.testDeleteDdpRole() {
        DdpRole obj = dod.getRandomDdpRole();
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to initialize correctly", obj);
        Integer id = obj.getRolId();
        Assert.assertNotNull("Data on demand for 'DdpRole' failed to provide an identifier", id);
        obj = ddpRoleService.findDdpRole(id);
        ddpRoleService.deleteDdpRole(obj);
        ddpRoleRepository.flush();
        Assert.assertNull("Failed to remove 'DdpRole' with identifier '" + id + "'", ddpRoleService.findDdpRole(id));
    }
    
}
