// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpGroupSetupDataOnDemand;
import com.agility.ddp.data.domain.DdpGroupSetupIntegrationTest;
import com.agility.ddp.data.domain.DdpGroupSetupRepository;
import com.agility.ddp.data.domain.DdpGroupSetupService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpGroupSetupIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpGroupSetupIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpGroupSetupIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpGroupSetupIntegrationTest: @Transactional;
    
    @Autowired
    DdpGroupSetupDataOnDemand DdpGroupSetupIntegrationTest.dod;
    
    @Autowired
    DdpGroupSetupService DdpGroupSetupIntegrationTest.ddpGroupSetupService;
    
    @Autowired
    DdpGroupSetupRepository DdpGroupSetupIntegrationTest.ddpGroupSetupRepository;
    
    @Test
    public void DdpGroupSetupIntegrationTest.testCountAllDdpGroupSetups() {
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to initialize correctly", dod.getRandomDdpGroupSetup());
        long count = ddpGroupSetupService.countAllDdpGroupSetups();
        Assert.assertTrue("Counter for 'DdpGroupSetup' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpGroupSetupIntegrationTest.testFindDdpGroupSetup() {
        DdpGroupSetup obj = dod.getRandomDdpGroupSetup();
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to initialize correctly", obj);
        Integer id = obj.getGrsId();
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to provide an identifier", id);
        obj = ddpGroupSetupService.findDdpGroupSetup(id);
        Assert.assertNotNull("Find method for 'DdpGroupSetup' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpGroupSetup' returned the incorrect identifier", id, obj.getGrsId());
    }
    
    @Test
    public void DdpGroupSetupIntegrationTest.testFindAllDdpGroupSetups() {
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to initialize correctly", dod.getRandomDdpGroupSetup());
        long count = ddpGroupSetupService.countAllDdpGroupSetups();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpGroupSetup', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpGroupSetup> result = ddpGroupSetupService.findAllDdpGroupSetups();
        Assert.assertNotNull("Find all method for 'DdpGroupSetup' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpGroupSetup' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpGroupSetupIntegrationTest.testFindDdpGroupSetupEntries() {
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to initialize correctly", dod.getRandomDdpGroupSetup());
        long count = ddpGroupSetupService.countAllDdpGroupSetups();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpGroupSetup> result = ddpGroupSetupService.findDdpGroupSetupEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpGroupSetup' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpGroupSetup' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpGroupSetupIntegrationTest.testSaveDdpGroupSetup() {
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to initialize correctly", dod.getRandomDdpGroupSetup());
        DdpGroupSetup obj = dod.getNewTransientDdpGroupSetup(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpGroupSetup' identifier to be null", obj.getGrsId());
        try {
            ddpGroupSetupService.saveDdpGroupSetup(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpGroupSetupRepository.flush();
        Assert.assertNotNull("Expected 'DdpGroupSetup' identifier to no longer be null", obj.getGrsId());
    }
    
    @Test
    public void DdpGroupSetupIntegrationTest.testDeleteDdpGroupSetup() {
        DdpGroupSetup obj = dod.getRandomDdpGroupSetup();
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to initialize correctly", obj);
        Integer id = obj.getGrsId();
        Assert.assertNotNull("Data on demand for 'DdpGroupSetup' failed to provide an identifier", id);
        obj = ddpGroupSetupService.findDdpGroupSetup(id);
        ddpGroupSetupService.deleteDdpGroupSetup(obj);
        ddpGroupSetupRepository.flush();
        Assert.assertNull("Failed to remove 'DdpGroupSetup' with identifier '" + id + "'", ddpGroupSetupService.findDdpGroupSetup(id));
    }
    
}
