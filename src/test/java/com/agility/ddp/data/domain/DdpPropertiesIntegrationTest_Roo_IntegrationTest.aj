// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpPropertiesDataOnDemand;
import com.agility.ddp.data.domain.DdpPropertiesIntegrationTest;
import com.agility.ddp.data.domain.DdpPropertiesPK;
import com.agility.ddp.data.domain.DdpPropertiesRepository;
import com.agility.ddp.data.domain.DdpPropertiesService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpPropertiesIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpPropertiesIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpPropertiesIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpPropertiesIntegrationTest: @Transactional;
    
    @Autowired
    DdpPropertiesDataOnDemand DdpPropertiesIntegrationTest.dod;
    
    @Autowired
    DdpPropertiesService DdpPropertiesIntegrationTest.ddpPropertiesService;
    
    @Autowired
    DdpPropertiesRepository DdpPropertiesIntegrationTest.ddpPropertiesRepository;
    
    @Test
    public void DdpPropertiesIntegrationTest.testCountAllDdpPropertieses() {
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to initialize correctly", dod.getRandomDdpProperties());
        long count = ddpPropertiesService.countAllDdpPropertieses();
        Assert.assertTrue("Counter for 'DdpProperties' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpPropertiesIntegrationTest.testFindDdpProperties() {
        DdpProperties obj = dod.getRandomDdpProperties();
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to initialize correctly", obj);
        DdpPropertiesPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to provide an identifier", id);
        obj = ddpPropertiesService.findDdpProperties(id);
        Assert.assertNotNull("Find method for 'DdpProperties' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpProperties' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DdpPropertiesIntegrationTest.testFindAllDdpPropertieses() {
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to initialize correctly", dod.getRandomDdpProperties());
        long count = ddpPropertiesService.countAllDdpPropertieses();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpProperties', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpProperties> result = ddpPropertiesService.findAllDdpPropertieses();
        Assert.assertNotNull("Find all method for 'DdpProperties' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpProperties' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpPropertiesIntegrationTest.testFindDdpPropertiesEntries() {
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to initialize correctly", dod.getRandomDdpProperties());
        long count = ddpPropertiesService.countAllDdpPropertieses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpProperties> result = ddpPropertiesService.findDdpPropertiesEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpProperties' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpProperties' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpPropertiesIntegrationTest.testSaveDdpProperties() {
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to initialize correctly", dod.getRandomDdpProperties());
        DdpProperties obj = dod.getNewTransientDdpProperties(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to provide a new transient entity", obj);
        try {
            ddpPropertiesService.saveDdpProperties(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpPropertiesRepository.flush();
        Assert.assertNotNull("Expected 'DdpProperties' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DdpPropertiesIntegrationTest.testDeleteDdpProperties() {
        DdpProperties obj = dod.getRandomDdpProperties();
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to initialize correctly", obj);
        DdpPropertiesPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DdpProperties' failed to provide an identifier", id);
        obj = ddpPropertiesService.findDdpProperties(id);
        ddpPropertiesService.deleteDdpProperties(obj);
        ddpPropertiesRepository.flush();
        Assert.assertNull("Failed to remove 'DdpProperties' with identifier '" + id + "'", ddpPropertiesService.findDdpProperties(id));
    }
    
}
