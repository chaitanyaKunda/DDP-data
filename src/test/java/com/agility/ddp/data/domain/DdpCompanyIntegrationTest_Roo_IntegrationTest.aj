// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpCompanyDataOnDemand;
import com.agility.ddp.data.domain.DdpCompanyIntegrationTest;
import com.agility.ddp.data.domain.DdpCompanyRepository;
import com.agility.ddp.data.domain.DdpCompanyService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpCompanyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpCompanyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpCompanyIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpCompanyIntegrationTest: @Transactional;
    
    @Autowired
    DdpCompanyDataOnDemand DdpCompanyIntegrationTest.dod;
    
    @Autowired
    DdpCompanyService DdpCompanyIntegrationTest.ddpCompanyService;
    
    @Autowired
    DdpCompanyRepository DdpCompanyIntegrationTest.ddpCompanyRepository;
    
    @Test
    public void DdpCompanyIntegrationTest.testCountAllDdpCompanys() {
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to initialize correctly", dod.getRandomDdpCompany());
        long count = ddpCompanyService.countAllDdpCompanys();
        Assert.assertTrue("Counter for 'DdpCompany' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpCompanyIntegrationTest.testFindDdpCompany() {
        DdpCompany obj = dod.getRandomDdpCompany();
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to initialize correctly", obj);
        String id = obj.getComCompanyCode();
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to provide an identifier", id);
        obj = ddpCompanyService.findDdpCompany(id);
        Assert.assertNotNull("Find method for 'DdpCompany' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpCompany' returned the incorrect identifier", id, obj.getComCompanyCode());
    }
    
    @Test
    public void DdpCompanyIntegrationTest.testFindAllDdpCompanys() {
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to initialize correctly", dod.getRandomDdpCompany());
        long count = ddpCompanyService.countAllDdpCompanys();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpCompany', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpCompany> result = ddpCompanyService.findAllDdpCompanys();
        Assert.assertNotNull("Find all method for 'DdpCompany' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpCompany' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpCompanyIntegrationTest.testFindDdpCompanyEntries() {
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to initialize correctly", dod.getRandomDdpCompany());
        long count = ddpCompanyService.countAllDdpCompanys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpCompany> result = ddpCompanyService.findDdpCompanyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpCompany' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpCompany' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpCompanyIntegrationTest.testSaveDdpCompany() {
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to initialize correctly", dod.getRandomDdpCompany());
        DdpCompany obj = dod.getNewTransientDdpCompany(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpCompany' identifier to be null", obj.getComCompanyCode());
        try {
            ddpCompanyService.saveDdpCompany(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpCompanyRepository.flush();
        Assert.assertNotNull("Expected 'DdpCompany' identifier to no longer be null", obj.getComCompanyCode());
    }
    
    @Test
    public void DdpCompanyIntegrationTest.testDeleteDdpCompany() {
        DdpCompany obj = dod.getRandomDdpCompany();
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to initialize correctly", obj);
        String id = obj.getComCompanyCode();
        Assert.assertNotNull("Data on demand for 'DdpCompany' failed to provide an identifier", id);
        obj = ddpCompanyService.findDdpCompany(id);
        ddpCompanyService.deleteDdpCompany(obj);
        ddpCompanyRepository.flush();
        Assert.assertNull("Failed to remove 'DdpCompany' with identifier '" + id + "'", ddpCompanyService.findDdpCompany(id));
    }
    
}
