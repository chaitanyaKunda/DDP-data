// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpRuleDataOnDemand;
import com.agility.ddp.data.domain.DdpRuleIntegrationTest;
import com.agility.ddp.data.domain.DdpRuleRepository;
import com.agility.ddp.data.domain.DdpRuleService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpRuleIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpRuleIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpRuleIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpRuleIntegrationTest: @Transactional;
    
    @Autowired
    DdpRuleDataOnDemand DdpRuleIntegrationTest.dod;
    
    @Autowired
    DdpRuleService DdpRuleIntegrationTest.ddpRuleService;
    
    @Autowired
    DdpRuleRepository DdpRuleIntegrationTest.ddpRuleRepository;
    
    @Test
    public void DdpRuleIntegrationTest.testCountAllDdpRules() {
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to initialize correctly", dod.getRandomDdpRule());
        long count = ddpRuleService.countAllDdpRules();
        Assert.assertTrue("Counter for 'DdpRule' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpRuleIntegrationTest.testFindDdpRule() {
        DdpRule obj = dod.getRandomDdpRule();
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to initialize correctly", obj);
        Integer id = obj.getRulId();
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to provide an identifier", id);
        obj = ddpRuleService.findDdpRule(id);
        Assert.assertNotNull("Find method for 'DdpRule' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpRule' returned the incorrect identifier", id, obj.getRulId());
    }
    
    @Test
    public void DdpRuleIntegrationTest.testFindAllDdpRules() {
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to initialize correctly", dod.getRandomDdpRule());
        long count = ddpRuleService.countAllDdpRules();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpRule', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpRule> result = ddpRuleService.findAllDdpRules();
        Assert.assertNotNull("Find all method for 'DdpRule' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpRule' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpRuleIntegrationTest.testFindDdpRuleEntries() {
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to initialize correctly", dod.getRandomDdpRule());
        long count = ddpRuleService.countAllDdpRules();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpRule> result = ddpRuleService.findDdpRuleEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpRule' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpRule' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpRuleIntegrationTest.testSaveDdpRule() {
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to initialize correctly", dod.getRandomDdpRule());
        DdpRule obj = dod.getNewTransientDdpRule(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpRule' identifier to be null", obj.getRulId());
        try {
            ddpRuleService.saveDdpRule(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpRuleRepository.flush();
        Assert.assertNotNull("Expected 'DdpRule' identifier to no longer be null", obj.getRulId());
    }
    
    @Test
    public void DdpRuleIntegrationTest.testDeleteDdpRule() {
        DdpRule obj = dod.getRandomDdpRule();
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to initialize correctly", obj);
        Integer id = obj.getRulId();
        Assert.assertNotNull("Data on demand for 'DdpRule' failed to provide an identifier", id);
        obj = ddpRuleService.findDdpRule(id);
        ddpRuleService.deleteDdpRule(obj);
        ddpRuleRepository.flush();
        Assert.assertNull("Failed to remove 'DdpRule' with identifier '" + id + "'", ddpRuleService.findDdpRule(id));
    }
    
}
