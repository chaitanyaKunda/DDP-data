// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpRateDataOnDemand;
import com.agility.ddp.data.domain.DdpRateIntegrationTest;
import com.agility.ddp.data.domain.DdpRateRepository;
import com.agility.ddp.data.domain.DdpRateService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpRateIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpRateIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpRateIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpRateIntegrationTest: @Transactional;
    
    @Autowired
    DdpRateDataOnDemand DdpRateIntegrationTest.dod;
    
    @Autowired
    DdpRateService DdpRateIntegrationTest.ddpRateService;
    
    @Autowired
    DdpRateRepository DdpRateIntegrationTest.ddpRateRepository;
    
    @Test
    public void DdpRateIntegrationTest.testCountAllDdpRates() {
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to initialize correctly", dod.getRandomDdpRate());
        long count = ddpRateService.countAllDdpRates();
        Assert.assertTrue("Counter for 'DdpRate' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpRateIntegrationTest.testFindDdpRate() {
        DdpRate obj = dod.getRandomDdpRate();
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to initialize correctly", obj);
        Integer id = obj.getRtsId();
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to provide an identifier", id);
        obj = ddpRateService.findDdpRate(id);
        Assert.assertNotNull("Find method for 'DdpRate' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpRate' returned the incorrect identifier", id, obj.getRtsId());
    }
    
    @Test
    public void DdpRateIntegrationTest.testFindAllDdpRates() {
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to initialize correctly", dod.getRandomDdpRate());
        long count = ddpRateService.countAllDdpRates();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpRate', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpRate> result = ddpRateService.findAllDdpRates();
        Assert.assertNotNull("Find all method for 'DdpRate' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpRate' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpRateIntegrationTest.testFindDdpRateEntries() {
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to initialize correctly", dod.getRandomDdpRate());
        long count = ddpRateService.countAllDdpRates();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpRate> result = ddpRateService.findDdpRateEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpRate' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpRate' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpRateIntegrationTest.testSaveDdpRate() {
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to initialize correctly", dod.getRandomDdpRate());
        DdpRate obj = dod.getNewTransientDdpRate(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpRate' identifier to be null", obj.getRtsId());
        try {
            ddpRateService.saveDdpRate(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpRateRepository.flush();
        Assert.assertNotNull("Expected 'DdpRate' identifier to no longer be null", obj.getRtsId());
    }
    
    @Test
    public void DdpRateIntegrationTest.testDeleteDdpRate() {
        DdpRate obj = dod.getRandomDdpRate();
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to initialize correctly", obj);
        Integer id = obj.getRtsId();
        Assert.assertNotNull("Data on demand for 'DdpRate' failed to provide an identifier", id);
        obj = ddpRateService.findDdpRate(id);
        ddpRateService.deleteDdpRate(obj);
        ddpRateRepository.flush();
        Assert.assertNull("Failed to remove 'DdpRate' with identifier '" + id + "'", ddpRateService.findDdpRate(id));
    }
    
}
