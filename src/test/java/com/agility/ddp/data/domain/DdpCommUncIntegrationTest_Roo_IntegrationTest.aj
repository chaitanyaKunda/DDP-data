// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpCommUncDataOnDemand;
import com.agility.ddp.data.domain.DdpCommUncIntegrationTest;
import com.agility.ddp.data.domain.DdpCommUncRepository;
import com.agility.ddp.data.domain.DdpCommUncService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpCommUncIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpCommUncIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpCommUncIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpCommUncIntegrationTest: @Transactional;
    
    @Autowired
    DdpCommUncDataOnDemand DdpCommUncIntegrationTest.dod;
    
    @Autowired
    DdpCommUncService DdpCommUncIntegrationTest.ddpCommUncService;
    
    @Autowired
    DdpCommUncRepository DdpCommUncIntegrationTest.ddpCommUncRepository;
    
    @Test
    public void DdpCommUncIntegrationTest.testCountAllDdpCommUncs() {
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to initialize correctly", dod.getRandomDdpCommUnc());
        long count = ddpCommUncService.countAllDdpCommUncs();
        Assert.assertTrue("Counter for 'DdpCommUnc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpCommUncIntegrationTest.testFindDdpCommUnc() {
        DdpCommUnc obj = dod.getRandomDdpCommUnc();
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to initialize correctly", obj);
        Long id = obj.getCunUncId();
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to provide an identifier", id);
        obj = ddpCommUncService.findDdpCommUnc(id);
        Assert.assertNotNull("Find method for 'DdpCommUnc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpCommUnc' returned the incorrect identifier", id, obj.getCunUncId());
    }
    
    @Test
    public void DdpCommUncIntegrationTest.testFindAllDdpCommUncs() {
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to initialize correctly", dod.getRandomDdpCommUnc());
        long count = ddpCommUncService.countAllDdpCommUncs();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpCommUnc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpCommUnc> result = ddpCommUncService.findAllDdpCommUncs();
        Assert.assertNotNull("Find all method for 'DdpCommUnc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpCommUnc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpCommUncIntegrationTest.testFindDdpCommUncEntries() {
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to initialize correctly", dod.getRandomDdpCommUnc());
        long count = ddpCommUncService.countAllDdpCommUncs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpCommUnc> result = ddpCommUncService.findDdpCommUncEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpCommUnc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpCommUnc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpCommUncIntegrationTest.testSaveDdpCommUnc() {
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to initialize correctly", dod.getRandomDdpCommUnc());
        DdpCommUnc obj = dod.getNewTransientDdpCommUnc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpCommUnc' identifier to be null", obj.getCunUncId());
        try {
            ddpCommUncService.saveDdpCommUnc(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpCommUncRepository.flush();
        Assert.assertNotNull("Expected 'DdpCommUnc' identifier to no longer be null", obj.getCunUncId());
    }
    
    @Test
    public void DdpCommUncIntegrationTest.testDeleteDdpCommUnc() {
        DdpCommUnc obj = dod.getRandomDdpCommUnc();
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to initialize correctly", obj);
        Long id = obj.getCunUncId();
        Assert.assertNotNull("Data on demand for 'DdpCommUnc' failed to provide an identifier", id);
        obj = ddpCommUncService.findDdpCommUnc(id);
        ddpCommUncService.deleteDdpCommUnc(obj);
        ddpCommUncRepository.flush();
        Assert.assertNull("Failed to remove 'DdpCommUnc' with identifier '" + id + "'", ddpCommUncService.findDdpCommUnc(id));
    }
    
}
