// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpCategorizedDetailDataOnDemand;
import com.agility.ddp.data.domain.DdpCategorizedDetailIntegrationTest;
import com.agility.ddp.data.domain.DdpCategorizedDetailRepository;
import com.agility.ddp.data.domain.DdpCategorizedDetailService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpCategorizedDetailIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpCategorizedDetailIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpCategorizedDetailIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpCategorizedDetailIntegrationTest: @Transactional;
    
    @Autowired
    DdpCategorizedDetailDataOnDemand DdpCategorizedDetailIntegrationTest.dod;
    
    @Autowired
    DdpCategorizedDetailService DdpCategorizedDetailIntegrationTest.ddpCategorizedDetailService;
    
    @Autowired
    DdpCategorizedDetailRepository DdpCategorizedDetailIntegrationTest.ddpCategorizedDetailRepository;
    
    @Test
    public void DdpCategorizedDetailIntegrationTest.testCountAllDdpCategorizedDetails() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to initialize correctly", dod.getRandomDdpCategorizedDetail());
        long count = ddpCategorizedDetailService.countAllDdpCategorizedDetails();
        Assert.assertTrue("Counter for 'DdpCategorizedDetail' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpCategorizedDetailIntegrationTest.testFindDdpCategorizedDetail() {
        DdpCategorizedDetail obj = dod.getRandomDdpCategorizedDetail();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to initialize correctly", obj);
        Integer id = obj.getCadId();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to provide an identifier", id);
        obj = ddpCategorizedDetailService.findDdpCategorizedDetail(id);
        Assert.assertNotNull("Find method for 'DdpCategorizedDetail' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpCategorizedDetail' returned the incorrect identifier", id, obj.getCadId());
    }
    
    @Test
    public void DdpCategorizedDetailIntegrationTest.testFindAllDdpCategorizedDetails() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to initialize correctly", dod.getRandomDdpCategorizedDetail());
        long count = ddpCategorizedDetailService.countAllDdpCategorizedDetails();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpCategorizedDetail', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpCategorizedDetail> result = ddpCategorizedDetailService.findAllDdpCategorizedDetails();
        Assert.assertNotNull("Find all method for 'DdpCategorizedDetail' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpCategorizedDetail' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpCategorizedDetailIntegrationTest.testFindDdpCategorizedDetailEntries() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to initialize correctly", dod.getRandomDdpCategorizedDetail());
        long count = ddpCategorizedDetailService.countAllDdpCategorizedDetails();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpCategorizedDetail> result = ddpCategorizedDetailService.findDdpCategorizedDetailEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpCategorizedDetail' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpCategorizedDetail' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpCategorizedDetailIntegrationTest.testSaveDdpCategorizedDetail() {
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to initialize correctly", dod.getRandomDdpCategorizedDetail());
        DdpCategorizedDetail obj = dod.getNewTransientDdpCategorizedDetail(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpCategorizedDetail' identifier to be null", obj.getCadId());
        try {
            ddpCategorizedDetailService.saveDdpCategorizedDetail(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpCategorizedDetailRepository.flush();
        Assert.assertNotNull("Expected 'DdpCategorizedDetail' identifier to no longer be null", obj.getCadId());
    }
    
    @Test
    public void DdpCategorizedDetailIntegrationTest.testDeleteDdpCategorizedDetail() {
        DdpCategorizedDetail obj = dod.getRandomDdpCategorizedDetail();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to initialize correctly", obj);
        Integer id = obj.getCadId();
        Assert.assertNotNull("Data on demand for 'DdpCategorizedDetail' failed to provide an identifier", id);
        obj = ddpCategorizedDetailService.findDdpCategorizedDetail(id);
        ddpCategorizedDetailService.deleteDdpCategorizedDetail(obj);
        ddpCategorizedDetailRepository.flush();
        Assert.assertNull("Failed to remove 'DdpCategorizedDetail' with identifier '" + id + "'", ddpCategorizedDetailService.findDdpCategorizedDetail(id));
    }
    
}
