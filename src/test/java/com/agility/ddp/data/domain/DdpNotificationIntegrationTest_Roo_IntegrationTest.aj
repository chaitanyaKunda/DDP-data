// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.agility.ddp.data.domain;

import com.agility.ddp.data.domain.DdpNotificationDataOnDemand;
import com.agility.ddp.data.domain.DdpNotificationIntegrationTest;
import com.agility.ddp.data.domain.DdpNotificationRepository;
import com.agility.ddp.data.domain.DdpNotificationService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DdpNotificationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DdpNotificationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DdpNotificationIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DdpNotificationIntegrationTest: @Transactional;
    
    @Autowired
    DdpNotificationDataOnDemand DdpNotificationIntegrationTest.dod;
    
    @Autowired
    DdpNotificationService DdpNotificationIntegrationTest.ddpNotificationService;
    
    @Autowired
    DdpNotificationRepository DdpNotificationIntegrationTest.ddpNotificationRepository;
    
    @Test
    public void DdpNotificationIntegrationTest.testCountAllDdpNotifications() {
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to initialize correctly", dod.getRandomDdpNotification());
        long count = ddpNotificationService.countAllDdpNotifications();
        Assert.assertTrue("Counter for 'DdpNotification' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DdpNotificationIntegrationTest.testFindDdpNotification() {
        DdpNotification obj = dod.getRandomDdpNotification();
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to initialize correctly", obj);
        Integer id = obj.getNotId();
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to provide an identifier", id);
        obj = ddpNotificationService.findDdpNotification(id);
        Assert.assertNotNull("Find method for 'DdpNotification' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DdpNotification' returned the incorrect identifier", id, obj.getNotId());
    }
    
    @Test
    public void DdpNotificationIntegrationTest.testFindAllDdpNotifications() {
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to initialize correctly", dod.getRandomDdpNotification());
        long count = ddpNotificationService.countAllDdpNotifications();
        Assert.assertTrue("Too expensive to perform a find all test for 'DdpNotification', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DdpNotification> result = ddpNotificationService.findAllDdpNotifications();
        Assert.assertNotNull("Find all method for 'DdpNotification' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DdpNotification' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DdpNotificationIntegrationTest.testFindDdpNotificationEntries() {
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to initialize correctly", dod.getRandomDdpNotification());
        long count = ddpNotificationService.countAllDdpNotifications();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DdpNotification> result = ddpNotificationService.findDdpNotificationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DdpNotification' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DdpNotification' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DdpNotificationIntegrationTest.testSaveDdpNotification() {
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to initialize correctly", dod.getRandomDdpNotification());
        DdpNotification obj = dod.getNewTransientDdpNotification(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DdpNotification' identifier to be null", obj.getNotId());
        try {
            ddpNotificationService.saveDdpNotification(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        ddpNotificationRepository.flush();
        Assert.assertNotNull("Expected 'DdpNotification' identifier to no longer be null", obj.getNotId());
    }
    
    @Test
    public void DdpNotificationIntegrationTest.testDeleteDdpNotification() {
        DdpNotification obj = dod.getRandomDdpNotification();
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to initialize correctly", obj);
        Integer id = obj.getNotId();
        Assert.assertNotNull("Data on demand for 'DdpNotification' failed to provide an identifier", id);
        obj = ddpNotificationService.findDdpNotification(id);
        ddpNotificationService.deleteDdpNotification(obj);
        ddpNotificationRepository.flush();
        Assert.assertNull("Failed to remove 'DdpNotification' with identifier '" + id + "'", ddpNotificationService.findDdpNotification(id));
    }
    
}
